def longestWord(word_list):
    word_len = []
    for n in word_list:
        word_len.append(len(n))
    word_len.sort()
    return word_len

orig_list = ["PHP", "Exercises", "Backend"]
index = longestWord(orig_list)
for i in orig_list:
    if index[-1]==len(i):
        print(i)
